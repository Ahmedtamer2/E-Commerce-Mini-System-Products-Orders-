# E-Commerce Mini System

نظام تجارة إلكترونية مصغّر مبني باستخدام **Laravel (Backend)** و **JWT Authentication**، مع لوحة إدارة خفيفة بـ **Vue.js**.
يوفّر النظام نموذجًا مبسطًا لإدارة المنتجات، السلة، والطلبات.

---

## 🚀 الميزات الأساسية

* تسجيل مستخدمين وتسجيل دخول باستخدام **JWT**
* إدارة كاملة للمنتجات (إضافة – تعديل – حذف – عرض)
* سلة مشتريات للمستخدم
* إنشاء طلب اعتمادًا على محتويات السلة
* التحقق من المخزون قبل إنشاء الطلب
* خصم الكميات من المخزون بعد الطلب
* إعادة رقم الطلب + إجمالي الفاتورة + تفاصيل العناصر
* مسح السلة بعد اكتمال الطلب

---

## 🧰 التقنيات المستخدمة

* Laravel 10+
* MySQL
* JWT Auth
* Vue.js (Dashboard بسيط)

---

## 📌 تثبيت المشروع

1. نسخ المشروع:

```bash
git clone <repo-url>
cd ecommerce-mini
```

2. تثبيت الاعتماديات:

```bash
composer install
```

3. إعداد ملف البيئة:

```bash
cp .env.example .env
php artisan key:generate
```

4. إعداد JWT:

```bash
php artisan jwt:secret
```

5. إعداد قاعدة البيانات:

```bash
php artisan migrate --seed
```

6. تشغيل الخادم:

```bash
php artisan serve
```

---

## 🔐 Authentication (JWT)

* `POST /api/auth/register`
* `POST /api/auth/login`
* `POST /api/auth/logout`
* `GET /api/auth/me`

كل ما بعد ذلك يتطلب **Bearer Token**.

---

## 🛒 Products API

* `GET /api/products`
* `POST /api/products`
* `PUT /api/products/{id}`
* `DELETE /api/products/{id}`

**ملاحظة:** إذا stock = 0 يظهر كمنتج *out_of_stock*.

---

## 🧺 Cart API

* `GET /api/cart`
* `POST /api/cart` (product_id, quantity)
* `DELETE /api/cart/{id}`

السعر يُقرأ من جدول المنتجات — لا يتم تخزينه داخل السلة.

---

## 🧾 Orders API

* `POST /api/orders` (address, phone)

**عند إنشاء الطلب:**

1. استخدام المنتجات الموجودة في السلة
2. التحقق من توفر الكمية
3. خصم المخزون
4. حفظ سعر المنتج وقت الطلب
5. إرجاع: رقم الطلب + الإجمالي + تفاصيل العناصر
6. مسح السلة

---

## 🗂 مخطط قاعدة البيانات (مختصر)

* Users
* Products
* Carts
* Orders
* Order Items

---

## 📄 ملاحظات

* المشروع تعليمي ومناسب للبدايات في بناء أنظمة E-Commerce.
* يمكن تطويره لاحقًا بإضافة: طرق دفع – كوبونات – شحن – Admin Panel كاملة.

---

جاهز لأي إضافة أو تنسيق تحبّه في الـ README. 😊
# E-Commerce Mini System

نظام تجارة إلكترونية مصغّر مبني باستخدام **Laravel (Backend)** و **JWT Authentication**، مع لوحة إدارة خفيفة بـ **Vue.js**.
يوفّر النظام نموذجًا مبسطًا لإدارة المنتجات، السلة، والطلبات.

---

## 🚀 الميزات الأساسية

* تسجيل مستخدمين وتسجيل دخول باستخدام **JWT**
* إدارة كاملة للمنتجات (إضافة – تعديل – حذف – عرض)
* سلة مشتريات للمستخدم
* إنشاء طلب اعتمادًا على محتويات السلة
* التحقق من المخزون قبل إنشاء الطلب
* خصم الكميات من المخزون بعد الطلب
* إعادة رقم الطلب + إجمالي الفاتورة + تفاصيل العناصر
* مسح السلة بعد اكتمال الطلب

---

## 🧰 التقنيات المستخدمة

* Laravel 10+
* MySQL
* JWT Auth
* Filament (Dashboard بسيط)

---

## 📌 تثبيت المشروع

1. نسخ المشروع:

```bash
git clone <repo-url>
cd ecommerce-mini
```

2. تثبيت الاعتماديات:

```bash
composer install
```

3. إعداد ملف البيئة:

```bash
cp .env.example .env
php artisan key:generate
```

4. إعداد JWT:

```bash
php artisan jwt:secret
```

5. إعداد قاعدة البيانات:

```bash
php artisan migrate --seed
```

6. تشغيل الخادم:

```bash
php artisan serve
```

---

## 🔐 Authentication (JWT)

* `POST /api/auth/register`
* `POST /api/auth/login`
* `POST /api/auth/logout`
* `GET /api/auth/me`

كل ما بعد ذلك يتطلب **Bearer Token**.

---

## 🛒 Products API

* `GET /api/products`
* `POST /api/products`
* `PUT /api/products/{id}`
* `DELETE /api/products/{id}`

**ملاحظة:** إذا stock = 0 يظهر كمنتج *out_of_stock*.

---

## 🧺 Cart API

* `GET /api/cart`
* `POST /api/cart` (product_id, quantity)
* `DELETE /api/cart/{id}`

السعر يُقرأ من جدول المنتجات — لا يتم تخزينه داخل السلة.

---

## 🧾 Orders API

* `POST /api/orders` (address, phone)

**عند إنشاء الطلب:**

1. استخدام المنتجات الموجودة في السلة
2. التحقق من توفر الكمية
3. خصم المخزون
4. حفظ سعر المنتج وقت الطلب
5. إرجاع: رقم الطلب + الإجمالي + تفاصيل العناصر
6. مسح السلة

---

## 🗂 مخطط قاعدة البيانات (مختصر)

* Users
* Products
* Carts
* Orders
* Order Items

---

## 📄 ملاحظات

* المشروع تعليمي ومناسب للبدايات في بناء أنظمة E-Commerce.
* يمكن تطويره لاحقًا بإضافة: طرق دفع – كوبونات – شحن – Admin Panel كاملة.

---

جاهز لأي إضافة أو تنسيق تحبّه في الـ README. 😊
